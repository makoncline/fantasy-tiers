# Local development defaults to file:./data/fantasy-history.db.
# Production uses fantasy-tiers-history in the dedicated fantasy-tiers Turso
# group. GitHub Actions uses a group write token; Vercel uses a separate
# group-scoped read-only token.
FANTASY_HISTORY_DATABASE_URL=
FANTASY_HISTORY_DATABASE_AUTH_TOKEN=
