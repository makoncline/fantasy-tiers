- [2026-08-05] Next.js 16.3 upgrade codemod scope: The default upgrade selection can add `instant = false` to routes even when `cacheComponents` is disabled. During a 16.3 upgrade, keep this codemod only for an intentional Cache Components adoption; otherwise remove the route opt-outs.
- [2026-09-02] Turbopack build port restriction: `next build` can fail in the Codex sandbox when the CSS loader tries to bind a local port. When the panic reports `binding to a port` with `EPERM`, verify with `pnpm exec next build --webpack` instead of changing application code.
- [2026-09-02] Scoring input validity: HTML number inputs default to whole-number steps, and a shared zero minimum rejects decimal and negative scoring defaults. When scoring fields share a number component, use `step="any"` without a minimum and verify that the preset can submit in a browser.
- [2026-09-03] Draft strategy evidence contract: The July research was broad, but its long brief used non-portable citation tokens and did not distinguish research from implemented policy. Before changing draft strategy, use `docs/draft-strategy-research-contract.md` to trace the rule to direct evidence, implementation status, and multi-seed validation.
- [2026-09-03] Public live-draft configuration: The owner preset is only the primary regression scenario and local mock default. During a live draft, build the league configuration from the selected Sleeper draft before loading aggregates or recommendations. Do not infer another user's settings from the owner preset.
- [2026-09-03] Owner-first format support: The internet app can load other Sleeper formats, but it does not need full strategy support for them. Detect unsupported format rules and show specific limitation messages. Optimize and validate deeply for the owner's league.
- [2026-09-03] Retired ranking sources: The active draft pipeline uses FantasyPros, Sleeper, and locally generated tiers only. Footballguys and Boris Chen code, data, and compatibility paths were removed. Do not add another provider without a clear role and a validation contract.
- [2026-09-03] TE completion safeguard: Penalizing every weak TE through round eight delayed the starter until rounds 9-10 and made the algorithm draft D/ST earlier. When improving TE quality, add an earlier quality-and-return window but keep a late roster-completion fallback.
- [2026-09-03] Sleeper beta draftboard network: The beta room uses `/beta/draft/nfl/<id>`, `sleeper.app/graphql`, and `api.sleeper.app`; it exposes separate visible rank, ADP, and projected-pick signals. For live testing, use role-based controls, keep public cache-busted draft REST as state authority, and sanitize raw HAR or CDP captures before any fixture is committed.
- [2026-09-03] Draft status and news separation: Structured Sleeper injury status already affects recommendation risk, while news loads only in the player preview. When adding freshness logic, compare news/status time with the ECR snapshot, prefetch only shortlist players, and show failed or stale news as unknown instead of healthy.
- [2026-09-03] Sleeper projection freshness boundary: Aggregate source health can be current because it uses player metadata while the ignored season-projection cache is stale. For projection-based draft value, use projection `last_modified`, validate required-stat coverage, and commit the normalized `sleeper-draft-projections.json` artifact.
- [2026-09-03] Sleeper news timestamp nesting: The projection response stores `news_updated` under `player`, not at the projection row root. Read `row.player.news_updated` when status/news freshness is part of draft data quality.
- [2026-09-03] Algorithm mock pool boundary: The canonical candidate map can contain players outside `bundleToSimPlayers`. Filter the recommendation candidates to the simulator player IDs or a valid recommendation can fail with `Unknown player` in alternate scoring formats.
- [2026-09-03] Strategy value units: Draft policy and evaluator thresholds must not compare raw values from different strategies. Use position rank or normalized components when a rule must work for ECR and starter-aware values.
- [2026-09-03] Projection calibration: For starter-aware draft value, preserve the exact-scoring projection curve within each position and assign it by trusted FantasyPros order. This keeps league-specific cross-position value without trusting one provider's exact player order.
- [2026-09-03] Strategy-neutral draft evaluation: Recommendation-time `staticValue` and `positionalValueRank` have strategy-specific units. When current and Beer+ are compared, grade QB and TE from the completed artifact's shared FantasyPros position rank and keep TE quality separate from mandatory roster validity.
- [2026-09-03] Sleeper replay configuration: The saved July 2026 Sleeper draft artifacts use schema v1 and omit exact scoring rules; several raw boards have no sibling artifact. When replaying historical boards, require a schema-v2 live artifact with matching picks and skip older boards instead of inferring a league format.
- [2026-09-03] Draft retrospective identity: FantasyPros ECR context is not the strategy decision for a Beer+ draft. Save a versioned canonical decision log during the draft, validate it against the result artifact, and label saved strategy ranking separately from market ranking.
- [2026-09-03] Sleeper scoring capability: Season kicker projections include 40-49 and 50-plus makes but not made field goals under 40 yards, so under-50 kicker scoring cannot be reconstructed exactly. Standard projected points can also include two-point conversions and hybrid IDP statistics. Preserve those fields for reconciliation, name the limitation, and never estimate missing kicker makes.
- [2026-09-03] One-position roster cap: Recommendation filtering alone does not protect manual mock picks or bot state transitions. Enforce the one-QB, one-TE, one-K, and one-D/ST maximum in the shared simulator pick transition and filter bot pools with the same policy.
- [2026-09-03] Retrospective snapshot binding: A player-ID signature cannot distinguish ranking or projection refreshes. Persist aggregate and projection timestamps in both the result artifact and decision log, then require every snapshot field to match before using saved recommendations.
- [2026-09-03] Imported league limitations: A normalized mock setup can silently hide superflex, restricted FLEX, IDP, TE-premium, or custom D/ST rules. Preserve the capability flag and show the same unsupported-format notices used by the live assistant.
- [2026-09-03] Standard D/ST round trip: A nonempty Sleeper scoring object treats omitted D/ST keys as zero, so a local mock that emits only offensive rules looks like custom D/ST scoring. When converting a standard mock to Sleeper details, emit the complete standard D/ST scoring map and test the round trip.
- [2026-09-04] Sleeper standalone scoring boundary: A standalone Sleeper mock can have no league ID and `scoring_settings: null`; `metadata.scoring_type` proves only standard, half, or full PPR, not a custom value such as 0.69. For an exact custom-scoring replay, save both the draft object and its league object and mark the import verified only when both parse.
- [2026-09-03] RB/WR reach evaluation: Adaptive FLEX assignment can change which picks count as bench depth when the roster mix changes. Compare mean positive reach per measured RB/WR depth pick and inspect pick sequences. Do not compare raw reach sums across strategies.
- [2026-09-03] Draft availability artifact fields: The canonical scorer can read injury status from aggregate rows, but the evaluator cannot audit saved drafts unless simulator players preserve status, news time, and ECR time. Keep these fields in `SimDraftPlayer` and result artifacts when availability policy changes.
- [2026-09-04] Canonical draft value input: The recommendation board now requires caller-supplied starter-aware values and has no ECR-only fallback. If scoring, projection freshness, or projection coverage fails, replace the draft board with a named incident. Do not reintroduce a runtime model selector.
- [2026-09-04] Draft readiness cohorts: Build likely-drafted cohorts from the union of FantasyPros ECR and real Sleeper ADP or board ranks. Keep the next-player reserve slice separate from the 100%-required expected pool, treat zero and 900-plus sentinel ranks as missing, and use prior reports only to identify regressions. A missing market rank is not a player incident when current FantasyPros ECR and Sleeper projection data are complete.
- [2026-09-04] Draft data incidents replace the board: The draft assistant must not show a prior board when FantasyPros, Sleeper, derived shards, or required player data is stale or incomplete. Show the named readiness incident and repair the pipeline before drafting.
- [2026-09-04] Pre-draft order boundary: Sleeper can return a complete pre-draft object with `draft_order: null`. Keep raw Val independent of the user slot, accept a URL-backed manual slot for Adj and recommendations, and let a published Sleeper order take precedence automatically.
- [2026-09-04] Draft-board diagnostic separation: Normal draft tables now require FantasyPros `rank_ave`, an undrafted row, and roster legality. The single Diagnostics mode shows only drafted or missing-ECR rows. Keep the overall table and dedicated position shards on the shared `draftBoardRows` filter so UI eligibility does not drift.
- [2026-09-04] Comeback demand calibration: Per-position roster-demand weights are not independent pick probabilities. Normalize QB, RB, WR, and TE demand for each intervening team, and bound its effect on the Sleeper ADP return estimate. Keep the proven room-pressure urgency internal, and do not present it as a probability.
- [2026-09-04] Reserve readiness severity: Core and expected-draft player gaps block aggregate publication. The next-three-round reserve cohort is warning-only, so a provider classification change cannot keep otherwise current draft data stale. Notify with the named reserve players after fresh data reaches production.
- [2026-09-04] Manual mock retrospective boundary: UI Save result writes a valid `mock-draft` artifact without an algorithm decision log. The retrospective currently rejects it because it treats all mock artifacts as algorithm runs. For manual table studies, preserve the saved artifact and pick notes, run direct roster evaluation, and do not relabel the source or invent canonical recommendation history.

- [2026-09-05] Draft experiment evidence: Source snapshot playerPoolSignature hashes IDs only, and aggregateGeneratedAt is a report creation time. For paired strategy runs, freeze aggregate file hashes and use version-2 decision logs for full component contributions and best legal Val. Do not treat ECR gains as independent outcome evidence.
- [2026-09-05] Mock opponent roster caps: simDraft getEligiblePlayersForSlot and makePick apply the owner one-QB/TE policy to bots too. When testing opponent return probabilities, allow backup selections in the experimental opponent pool and label checks against current bots as synthetic evidence.

- [2026-09-05] Prospective Sleeper capture: Use a ten-minute pick clock for browser-controlled evidence runs. Browser control can take 20-30 seconds per call, and a 60-second clock caused two forced picks. Verify our Queue Auto-pick switch is off separately from the room CPU Autopick setting. The room CPU setting also stops unclaimed teams. Save each prediction before selecting a player, and exclude any failed-control board.
- [2026-09-05] Shared choice panel visibility: The mock room passed `showRecommendations={false}` from an earlier table-only study. This hid the shared Decision Board even when canonical recommendations were ready. The choice-assistant release removes that override. Check the mock and live render gates when validating advice UI.
- [2026-09-05] Choice wait boundary: `recommendationBoard.nextPick` is inclusive and can be the pick now on the clock. Comparison waiting text must call `getNextPickForSlot` with currentPick + 1, as the scoring model does, or the final-pick explanation can invent another turn.
- [2026-09-05] Sleeper mock cloning: A cloned completed mock can report pre_draft while retaining all original picks. For prospective tests, create an empty room and verify zero public picks before registration. Do not treat a new draft ID as proof of fresh evidence.
- [2026-09-05] Sleeper settings and paused CPU control: updateDraftSettings requires the complete settings object. The commissioner draftPlayer and draftCpuPick actions can make picks while paused. For bounded CPU tests, verify each pick through cache-busted public REST, record this control mode, and mark complete only after all expected picks exist.
- [2026-09-05] Prospective evidence versus owner adherence: Validate the saved prior prefix and actual selection used to condition the forecast. Record default-following separately. A non-default choice is not an evidence failure; changing the conditioned selection is.

- [2026-09-06] Scenario FLEX demand: The v2 packet also omitted FLEX when it rebuilt room-wide needs. When changing the next-pick adapter, preserve FLEX separately from player positions and test the room total, not only the owner roster.
- [2026-09-06] ESPN live draft capture: The draftInit HTTP response can retain empty pick slots after live picks. Record DevTools Network with All selected and replay the fantasydraft.espn.com WebSocket INIT and SELECTED frames. A Fetch/XHR-only HAR does not contain the live board. Chrome's sanitized HAR can still contain WebSocket authentication bodies; keep raw captures outside Git and never print those frames.
- [2026-09-06] Comparison card pick actions: The inspection pool can contain a high-Val player outside `decisionRows`. Local card picks must use the canonical eligible candidate's player ID, not look up the compact table slice. Otherwise a visible alternative can silently lack a Pick button.
- [2026-09-06] ESPN direct browser authentication: CORS allows credentialed app-origin reads, but Chrome omits SWID and espn_s2 with SameSiteUnspecifiedTreatedAsLax, so draftSecurity and draftInit return 401. A captured practice token can open a direct localhost WebSocket and decode the full draft through the shared protocol. Test bootstrap and live transport separately; do not infer a working ID-only login from a successful preflight or token-backed stream.
- [2026-09-06] Cross-pool tier labels: FLEX shard tiers are not position tiers, and position shard tiers are not overall tiers. Keep each pool's own rows, but use the dedicated position-tier map for FLEX labels and ALL metadata for preview overall tiers. The expanded table must retain the compact table's tier semantics.
- [2026-09-07] ESPN duplicate draft connection: A token-backed companion WebSocket receives the full board but disconnects the same account's ESPN draft tab. A cache-busted mDraftDetail still returned empty slots with six live picks. Verify simultaneous sessions before enabling companion sync; the current copy/paste setup checks account access only. ESPN JOIN uses literal game:league:team:member:security token separators; percent-encoded colons/braces failed.
- [2026-09-06] Direct versus shared draft demand: `draftContext.positionOutlook[].leagueStarterSlotsRemaining` includes all open FLEX slots for each eligible position. UI direct counts must use `draftContext.room.leagueStarterSlotsRemaining[position]` and show `.FLEX` separately, counted once. Do not label the combined outlook as direct TE/RB/WR starter need.

- [2026-09-07] ESPN reader relay: Observe the draft page's existing WebSocket at document_start; a second authenticated ESPN socket disconnects the drafting tab. Decode and strip private fields before crossing into the extension or relay. Production relay writes need a dedicated database; the rankings database token is read only. Use `pnpm run espn:build-reader` and the isolated `scripts/espn/verify-reader.mjs` check before distributing an unpacked build.

- [2026-09-06] Codex review CLI version: The PATH CLI v0.146.0 cannot review with gpt-6-astra and returns a newer-version-required error. The app-bundled /Applications/ChatGPT.app/Contents/Resources/codex v0.153.4 works. Use the bundled CLI with the same model and review target when this error occurs.
- [2026-09-07] ESPN reader first pairing: Chrome's optional localhost permission prompt can close the extension popup before pairing completes. After allowing access, reopen the reader and select Connect assistant again if needed. Verify the Paired status before reloading ESPN. The installed reader then delivered live picks to two assistant views without disconnecting ESPN.

- [2026-09-07] ESPN Reader release target: The reader build embeds its assistant origin and grants that host in its manifest. Use ESPN_ASSISTANT_ORIGIN=http://localhost:3006 for isolated tests; rebuild without the variable before sharing dist/espn-reader.zip. The popup-as-tab browser harness supplies the selected ESPN tab and must wait for reader-ready before asserting no reload.

- [2026-09-07] FantasyPros free API truncation: A preseason all-offense request reported 543 players but returned only 10 QBs. The key notice limits usage to 50/day; the older terms PDF says 100/day. Use the stricter key limit, retain complete browser projection captures, and read docs/fantasypros-api-reference.md before API work.

- [2026-09-07] FP native refresh: The existing website scraper and FP_COOKIE helper returned 526 full season projection rows on GitHub Actions. Use the draft-only staged publication wrapper; weekly ECR fetches do not refresh season projections. Use fetch-data.yml projections_only mode to verify access and failure preservation without publishing production data.
- [2026-09-07] Draft source toggle: The source comparison already builds both native boards. Reuse sourceViews and selectDraftSource; keep the selected source out of the live/mock calculation memo dependencies. Otherwise toggling reruns readiness and scoring even though both calculations already exist.
- [2026-09-07] Mock route build mode: `/mock-draft` calls `notFound()` in production. Browser rehearsal must use `next dev` (for example a separate port with `--webpack`), even when `/draft-assistant` is served successfully by `next start`.
- [2026-09-07] Shared draft section links and ESPN sessions: Shared navigation owns the URL hash. ESPN consumes its initial read key into tab sessionStorage and clears the hash. Keep session state out of section anchors, and test navigation plus reload when changing the shared workspace. Static context consumers also need draftSlot, pickFeed, and working refetch handlers.

- [2026-09-07] Local release build worker restriction: Turbopack CSS compilation can fail to bind its worker port even after escalation on this Mac. The supported `pnpm exec next build --webpack` path passed. Verify and serve that exact build, and run typecheck after build rather than while Next rewrites generated types.
- [2026-09-07] Sleeper league mock setup: Opening a league's draftboards URL directly can leave New Mock without its draft type. Open the league pre-draft page and use Practice drafting first, then New Mock. Verify metadata.league_id and the assistant's resolved scoringRules; a mock's generic PPR label does not establish the league's 0.69-PPR map.
- [2026-09-07] ESPN browser transport and scoring: Reader 2.0 uses Chrome session storage and tab messaging; the earlier read-key/sessionStorage note is obsolete. Keep both tabs in one profile. Use the existing ESPN leaguePoints artifact with actual appliedTotal projections and the combined view; the Sleeper artifact rejects ESPN D/ST scoring. Keep unknown projections missing and never change shared Sleeper scoring to make an ESPN fixture pass.

- [2026-09-07] Draft timing audit: `urgencyScore` includes explicit room demand, but `components.timing` does not. The ADJ demand component is separate. When testing timing changes, inspect `rawScores` rather than inferring overlap from the display urgency summary.
- [2026-09-07] Position leader stability: The selected-source ADJ rule can switch an undrafted position leader without a source refresh. Existing risk differences can reverse close score gaps, and equal source ranks retain timing differences. When checking stability, compare consecutive owner turns and separate drafted players, ineligible positions, rank ties, and risk differences. Use scripts/draft/report-position-stability.py with recorded position leaders.
- [2026-09-07] Actual draft replay inputs: The frozen actual-draft sleeper-picks.json has 176 picks; final-picks.json has all 180. Travis Hunter at pick 175 is absent from the offensive candidate pool and occurs after the owner's final pick at 172. Validate completeness and candidate coverage before replay; preserve this late observed pick without inventing a WR rank or projection.
- [2026-09-07] ESPN D/ST scoring overrides: The live league-specific practice uses base zero with position 16 overrides for normal D/ST scoring, such as stat 89 at 5 points and stat 123 at -1. Permit these in the ESPN adapter because its leaguePoints projections already contain ESPN appliedTotal. Keep unsupported offensive position overrides blocked; do not change shared Sleeper scoring.

- [2026-09-07] ESPN source boundary: The owner confirmed ESPN is only draft state, rules, and player identity; retain Sleeper and FantasyPros projections and ranks through the same source selector. Do not create an ESPN projection artifact. D/ST and kicker values use explicitly labeled standard Sleeper references, as approved by the owner.

- [2026-09-07] ESPN unranked picks and undo: Live practice drafted Travis Hunter as a WR although the shared ranked pool omitted him. Keep known ESPN unranked selections as draft-only identities with null ranking/projection fields. ESPN UNDONE clears the named pick and all later picks; clear the full suffix to avoid stale pick gaps.

- [2026-09-07] Shared draft display parity: Sleeper, ESPN, and local mocks must use useDraftAssistantContextValue for row enrichment, roster slots, and drafted values. The previous duplicate ESPN/mock path omitted static values for drafted players, so Show drafted changed counts but hid the rows below the visible slice. Keep platform providers limited to loading and normalized inputs; verify rendered parity with identical scoring, including specialist reference rules.

- [2026-09-08] ESPN undo index: The live ESPN client onUndoneMessage adds one to the UNDONE index; INIT pickNumber is one-based. Convert the index before clearing that pick and its suffix. Verify the retained previous pick in browser tests.

- [2026-09-08] ESPN idle socket health: A paused or quiet draft can send no frames for more than the 15-second freshness window. Stamp each main-world health snapshot from the existing socket readyState; do not use the last draft frame time as proof of a lost connection. Keep transport-age and closed-socket checks.

- [2026-09-08] ESPN Chrome row labels: The browser control can retain an old accessible row name after ESPN filters the player grid. Verify the filtered grid text, select its sole action, and verify the retained pick through the sanitized Reader snapshot before retrying any timeout. A timeout can follow a successful pick.
- [2026-09-08] Tier experiment input freshness: Published aggregate FP ranking fields can reconstruct the current local tier inputs while ignored raw FP files in another checkout can be older. Before a tier comparison, freeze published data and require regenerated baseline tiers to match each position shard within its tiering limit.
